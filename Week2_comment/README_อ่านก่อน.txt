ไฟล์ชุดนี้เพิ่มคอมเมนต์ภาษาไทยอธิบายโค้ดทีละบรรทัด โดยไม่แก้ Logic เดิม
เน้นคำสำคัญ: async def, coroutine object, await, asyncio.create_task(), Event Loop, gather(), Thread, Process, start(), join(), PID และการทำงานแบบ Sequential/Concurrent
จำนวนไฟล์ .py: 13 ไฟล์
ตรวจสอบ Syntax ด้วย py_compile แล้ว: ผ่านทุกไฟล์
